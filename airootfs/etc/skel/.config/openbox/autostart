# 1. Start the Polkit Agent so pkexec can actually show a popup (if needed)
/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &

# 2. Give the agent a second to breathe
sleep 1

# 3. Launch Calamares
pkexec calamares &