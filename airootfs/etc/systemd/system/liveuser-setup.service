[Unit]
Description=Setup live user
After=systemd-user-sessions.service

[Service]
Type=oneshot
ExecStart=/usr/bin/useradd -m -G wheel -s /usr/bin/bash liveuser
ExecStart=/usr/bin/passwd -d liveuser
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
